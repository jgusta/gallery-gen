doctype
html(lang=en)
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title
      data.title
    link(href="style.css", rel="stylesheet")
  body
    .container
      each value in data.pageData
        include ./thumb.pug
